{% extends 'base.html.twig' %}


{% block title %}Stats{% endblock %}

{% block content %}

<h1>Stat de l'agence de {{ stat.getAgenceId().getVille()}}</h1>
<br>
    <table class="table text-center">
      <thead class="thead-dark">
    <tr>

      <th scope="col">ChiffreAffaire</th>
      <th scope="col">MatierePremiere</th>
      <th scope="col">Benefice</th>
    </tr>
  </thead>
        <tbody>
            <tr>
              

          
              
                <td class="text-warning">{{ AllStats[stat.id - 1][1]  }}€</td>
           
               
                <td class="text-danger">{{ AllStats[stat.id - 1][2]  }}€</td>
           
                <td class="text-success">{{ AllStats[stat.id - 1][3]  }}€</td>
            </tr>
        </tbody>
    </table>

   {# <a href="{{ path('app_stats_index') }}">back to list</a>

    <a href="{{ path('app_stats_edit', {'id': stat.id}) }}">edit</a>

    {{ include('stats/_delete_form.html.twig') }}#}
{% endblock %}
